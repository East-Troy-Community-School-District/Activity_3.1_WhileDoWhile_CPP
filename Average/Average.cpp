/*
Average
Pawelski
4/16/2023
C++
*/

#include <iostream>

int main()
{
    int number, sum = 0, count = 0;
    double average;
    std::string repeat = "Y";
    while (repeat == "Y")
    {
        std::cout << "Enter a number >> ";
        std::cin >> number;
        // Add the entered number to sum.
        // Increase count by 1.
        // Ask the user if they would like to enter another number.
        // Store the user's response in repeat.
    }
    // Calculate the average.
    std::cout << "The average is " << average << ".\n";
    return 0;
}